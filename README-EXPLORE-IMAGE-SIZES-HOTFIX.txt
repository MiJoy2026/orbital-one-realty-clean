Explore Page Image Performance Hotfix

Adds responsive `sizes` props to every Next.js Image using `fill` on the Explore page.

This removes the development warnings and allows Next.js to serve appropriately sized image variants for mobile, tablet, and desktop screens.

Updated file:
- app/explore/page.tsx

No layout, copy, routes, pricing, database, or inventory logic is changed.
