Orbital One LunaScape exact-parcel renderer TypeScript hotfix

This package replaces the two renderer files with versions that do not use sharp as a TypeScript namespace.

Files:
- lib/lroc-terrain-renderer.ts
- lib/property-image-renderer.ts

No database migration or npm install is required.
