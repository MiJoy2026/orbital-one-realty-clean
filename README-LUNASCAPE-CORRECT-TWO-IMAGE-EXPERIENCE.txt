Orbital One / LunaScape Correct Two-Image Experience

Customer-facing images:
1. Your Place on the Moon
   - Existing exact-parcel renderer with the thin corrected boundary.
2. Your LunaScape Property
   - Dedicated curated virtual-scene renderer.
   - Rural/isolated: approved realistic open-terrain image.
   - Town: dedicated LunaScape town-community artwork.
   - City: dedicated LunaScape city-community artwork.
   - Nearby attractions may be blended faintly into the background as local context.

The virtual image no longer uses the tiled crescent/postcard/perspective-strip renderer.
The gallery now has exactly two image cards and exactly one download button per image.

New folder:
public/lunascape/virtual-scenes/

New renderer:
lib/lunascape-virtual-renderer.ts

No Prisma migration is required.
No npm install is required.
